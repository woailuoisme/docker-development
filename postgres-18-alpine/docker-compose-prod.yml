services:
  postgres:
    container_name: postgres
    image: jiaoio/postgres:18.4-alpine3.24
    restart: always
    ports:
      - "45432:5432"
    environment:
      - POSTGRES_USER=${POSTGRES_USER}
      - POSTGRES_PASSWORD=${POSTGRES_PASSWORD}
      - POSTGRES_DB=${POSTGRES_DB}
    volumes:
      - ${DATA_PATH}postgres18:/var/lib/postgresql
      - ${CONFIG_PATH}postgres-18-alpine/docker-entrypoint-initdb.d:/docker-entrypoint-initdb.d
    networks:
      - backend
      - frontend
