# https://github.com/mholt/caddy-l4/blob/master/docs/examples/socks_proxy.md
# The SOCKS Proxy example shows a proxy forwarding SOCKSv4 to a remote server and handling SOCKSv5 directly in Caddy. 
# Note: it only allows connections from a specific network and requires a username and a password for SOCKSv5.

# Layer4 配置示例（已禁用）
# 如果需要启用，请将此配置移到 Caddyfile 主文件的全局配置块之后
# {
# 	layer4 {
# 		:443 {
# 			# 如果你想在 443 端口也支持 Redis，只能通过 SNI 区分，且必须使用 TLS
# 			@redis sni redis.{$SITE_ADDRESS}
# 			route @redis {
# 				tls
# 				proxy redis:6379
# 			}
# 		}
# 	}
# }

# 简单的 Redis 代理示例（已禁用）
# layer4 {
#     :16379 {
#         route {
#             # 所有连接直接转发到 redis
#             proxy {
#                 upstream redis:6379
#             }
#         }
#     }
# }
