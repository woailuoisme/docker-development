name: Build Group - Infrastructure Services

on:
  workflow_dispatch:
    inputs:
      # === 控制开关 ===
      build_all:
        description: '🚀 构建所有基础服务'
        type: boolean
        default: false
      
      build_goaccess:
        description: '构建 GoAccess'
        type: boolean
        default: false
      build_authelia:
        description: '构建 Authelia'
        type: boolean
        default: false
      build_caddy:
        description: '构建 Caddy'
        type: boolean
        default: false
      build_telegraf:
        description: '构建 Telegraf'
        type: boolean
        default: false
      build_meilisearch:
        description: '构建 Meilisearch'
        type: boolean
        default: false
      build_minio:
        description: '构建 MinIO'
        type: boolean
        default: false
      build_rabbitmq:
        description: '构建 RabbitMQ'
        type: boolean
        default: false
      build_pgbouncer:
        description: '构建 PgBouncer'
        type: boolean
        default: false
      
      # === 通用参数 ===
      tag:
        description: '镜像标签 (e.g., latest)'
        required: true
        default: 'latest'

      # === 版本参数 ===
      authelia_version:
        description: 'Authelia 版本'
        required: false
        default: '4.39'
      meili_version:
        description: 'Meilisearch Version'
        required: false
        default: 'v1.33'
      goaccess_version:
        description: 'GoAccess 版本'
        required: false
        default: 'latest'
      caddy_base_version:
        description: 'Caddy 基础版本'
        required: false
        default: 'latest'
      telegraf_version:
        description: 'Telegraf 版本'
        required: false
        default: '1.37-alpine'
      minio_version:
        description: 'MinIO 版本'
        required: false
        default: 'latest'
      rabbitmq_version:
        description: 'RabbitMQ 版本'
        required: false
        default: '4.2-management'
      pgbouncer_version:
        description: 'PgBouncer 版本'
        required: false
        default: 'latest'

env:
  REGISTRY_DOCKERHUB: docker.io/jiaoio
  REGISTRY_REDHAT: quay.io/jiaoio

jobs:
  matrix-builder:
    runs-on: ubuntu-latest
    outputs:
      matrix: ${{ steps.set-matrix.outputs.matrix }}
    steps:
      - id: set-matrix
        run: |
          MATRIX_JSON='[]'
          
          add_task() {
            local name=$1
            local context=$2
            local image=$3
            local version=$4
            MATRIX_JSON=$(echo $MATRIX_JSON | jq -c ". + [{\"name\": \"$name\", \"context\": \"$context\", \"image\": \"$image\", \"version\": \"$version\"}]")
          }
          
          BUILD_ALL=${{ inputs.build_all }}
          
          [[ "$BUILD_ALL" == "true" || "${{ inputs.build_goaccess }}" == "true" ]] && add_task "goaccess" "./go-access" "goaccess" "${{ inputs.goaccess_version }}"
          [[ "$BUILD_ALL" == "true" || "${{ inputs.build_authelia }}" == "true" ]] && add_task "authelia" "./authelia" "authelia" "${{ inputs.authelia_version }}"
          [[ "$BUILD_ALL" == "true" || "${{ inputs.build_caddy }}" == "true" ]] && add_task "caddy" "./caddy" "caddy" "${{ inputs.caddy_base_version }}"
          [[ "$BUILD_ALL" == "true" || "${{ inputs.build_telegraf }}" == "true" ]] && add_task "telegraf" "./telegraf" "telegraf" "${{ inputs.telegraf_version }}"
          [[ "$BUILD_ALL" == "true" || "${{ inputs.build_meilisearch }}" == "true" ]] && add_task "meilisearch" "./meilisearch" "meilisearch" "${{ inputs.meili_version }}"
          [[ "$BUILD_ALL" == "true" || "${{ inputs.build_minio }}" == "true" ]] && add_task "minio" "./minio" "minio" "${{ inputs.minio_version }}"
          [[ "$BUILD_ALL" == "true" || "${{ inputs.build_rabbitmq }}" == "true" ]] && add_task "rabbitmq" "./rabbitmq" "rabbitmq" "${{ inputs.rabbitmq_version }}"
          [[ "$BUILD_ALL" == "true" || "${{ inputs.build_pgbouncer }}" == "true" ]] && add_task "pgbouncer" "./pgbouncer" "pgbouncer" "${{ inputs.pgbouncer_version }}"
          
          echo "matrix={\"include\":$MATRIX_JSON}" >> $GITHUB_OUTPUT

  build-infra:
    needs: matrix-builder
    if: ${{ fromJson(needs.matrix-builder.outputs.matrix).include[0] != null }}
    runs-on: ubuntu-latest
    strategy:
      fail-fast: false
      matrix: ${{ fromJson(needs.matrix-builder.outputs.matrix) }}
    
    name: Build ${{ matrix.name }}
    
    steps:
      - name: Checkout code
        uses: actions/checkout@v4

      - name: Set up QEMU
        uses: docker/setup-qemu-action@v3

      - name: Set up Docker Buildx
        uses: docker/setup-buildx-action@v3

      - name: Login to Registries
        run: |
          echo "${{ secrets.DOCKERHUB_TOKEN }}" | docker login -u "${{ secrets.DOCKERHUB_USERNAME }}" --password-stdin
          echo "${{ secrets.REDHAT_REGISTRY_TOKEN }}" | docker login quay.io -u "${{ secrets.REDHAT_REGISTRY_USERNAME }}" --password-stdin

      - name: Build and push
        uses: docker/build-push-action@v6
        with:
          context: ${{ matrix.context }}
          platforms: linux/amd64,linux/arm64
          push: true
          build-args: |
             GOACCESS_VERSION=${{ matrix.version }}
             AUTHELIA_VERSION=${{ matrix.version }}
             CADDY_BASE_VERSION=${{ matrix.version }}
             TELEGRAF_VERSION=${{ matrix.version }}
             MEILI_VERSION=${{ matrix.version }}
             MINIO_VERSION=${{ matrix.version }}
             RABBITMQ_VERSION=${{ matrix.version }}
             PGBOUNCER_VERSION=${{ matrix.version }}
             CHANGE_SOURCE=false
          tags: |
            ${{ env.REGISTRY_DOCKERHUB }}/${{ matrix.image }}:${{ matrix.version }}-${{ inputs.tag }}
            ${{ env.REGISTRY_DOCKERHUB }}/${{ matrix.image }}:${{ matrix.version }}
            ${{ env.REGISTRY_REDHAT }}/${{ matrix.image }}:${{ matrix.version }}-${{ inputs.tag }}
            ${{ env.REGISTRY_REDHAT }}/${{ matrix.image }}:${{ matrix.version }}
